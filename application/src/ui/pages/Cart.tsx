import { HydratedCart } from '../components/cart';

export default () => {
    return (
        <div className="lg:w-content mx-auto w-full min-h-[90vh]">
            <HydratedCart />
        </div>
    );
};
